# Colab already provides PyTorch. Pin only the packages whose behavior affects
# raster IO and configuration parsing.
rasterio==1.4.3
PyYAML==6.0.3
pandas>=2.1,<3
scipy>=1.11,<2
tqdm>=4.66,<5
matplotlib>=3.8,<4
geopandas==1.0.1
pyogrio==0.10.0
